project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("test_sgf.adb", "main.adb");

   package Compiler is
      for Switches ("ada") use ("-gnata");
   end Compiler;

end Default;

